import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  formWrapper: {
    marginTop: '40px',
  },
  updateButton: {
    marginTop: '20px',
    marginLeft: '30%',
    marginRight: '30%',
    width: '40%',
  },
}));

export default useStyles;
