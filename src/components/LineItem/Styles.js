import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  quantityProduct: {
    width: '40px'
  },
  labelQuantiry: {
    marginTop: '10px'
  }
});

export default useStyles;
