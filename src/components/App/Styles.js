import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  body: {
    height: '100%',
    margin: '0px',
    padding: '0px',
    backgroundColor: 'red'
  }
});

export default useStyles;
