import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  map: {
    marginTop: '30px',
    height: '400px',
    width: '100%'
  }
}));

export default useStyles;
