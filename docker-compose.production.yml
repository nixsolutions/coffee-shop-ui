version: '3.7'
services:
  client:
    build:
      context: .
      dockerfile: Dockerfile.production
    ports:
      - '3000:3000'
    networks:
      - coffee-shop-ui
networks:
  coffee-shop-ui:
    name: coffee-shop-ui
