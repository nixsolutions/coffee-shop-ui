version: '3.7'
services:
  client:
    build:
      context: .
      dockerfile: Dockerfile.production
    ports:
      - '8000:8000'
    networks:
      - coffee-shop-ui
networks:
  coffee-shop-ui:
    name: coffee-shop-ui
